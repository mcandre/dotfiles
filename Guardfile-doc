guard :shell do
  watch(%r{(pom\.xml)|(src/(main|test)/.+\.(thrift|java))}) do |m|
    title = 'Javadoc'
    msg = `mvn javadoc:javadoc`
    status = ($?.success? && :success) || :failed

    n msg, title, status
    "-> #{msg}"
  end
end
