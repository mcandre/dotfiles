# Pass aliases to sudo commands
alias sudo='sudo '

# Show details
alias ll='ls -Ahl'

# Colorize soft links
alias ls='gls --color=auto'

# Hide progress bar
alias curl='curl -s'

# Show progress bar
alias rsync='rsync --info=progress2 -a'
