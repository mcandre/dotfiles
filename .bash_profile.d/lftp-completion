# mkdir -p $HOME/.bash_profile.d/
# ln -s .../lftp-completion $HOME/.bash_profile.d/lftp-completion
# echo 'for f in $HOME/.bash_profile.d/* ; do [[ -s $f ]] && source "$f" ; done' >> $HOME/.bash_profile

# lftp-completion
# See https://raw.githubusercontent.com/mcandre/lftp-completion/master/lftp-completion.sh
. $HOME/lftp-completion.sh
