# mkdir -p $HOME/.bash_profile.d/enabled/
# ln -s .../lftp-completion $HOME/.bash_profile.d/enabled/lftp-completion.sh
# echo 'for f in "$HOME/.bash_profile.d/enabled/"* ; do . "$f" ; done' >> $HOME/.bash_profile

# lftp-completion
# See https://raw.githubusercontent.com/mcandre/lftp-completion/master/lftp-completion.sh
. "$HOME/lftp-completion.sh"
