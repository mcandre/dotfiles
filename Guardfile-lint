guard :shell do
  watch(%r{^pom\.xml|src/(main|test)/.+\.(java|thrift)}) do |m|
    title = 'Lint'
    eager 'mvn compile && mvn checkstyle:checkstyle && mvn pmd:check'
    status = ($?.success? && :success) || :failed
    n '', title, status
    ''
  end
end
