#!/bin/sh
#
# Install:
# cp global.envrc "$HOME/.envrc"
#
# Uninstall:
# rm "$HOME/.envrc"

PATH_add "${HOME}/.kickers"

# shellcheck source=/dev/null
export PATH="${ASDF_DATA_DIR:-$HOME/.asdf}/shims:$PATH"
